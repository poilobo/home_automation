- hosts: localhost
  connection: local
  become: true
  
  tasks: 
  - name: Install htop
    apt:
      name: htop
  - name: Install vim
    apt:
      name: vim
  - name: Install tmux
    apt:
      name: tmux
  - name: Install python-is-python3
    apt:
      name: python-is-python3
